import { View } from '@eventlink/web-components';
import { AppLayout } from '@eventlink/web-layouts';

export function ViewPage() {
  return (
    <AppLayout title="View">
      <View />
    </AppLayout>
  );
}

export default ViewPage;
